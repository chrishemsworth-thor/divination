{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply edgeiq-db",
    "migrate:local": "wrangler d1 migrations apply edgeiq-db --local",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "stripe": "^15.12.0",
    "resend": "^3.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240718.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.63.2"
  }
}
